<?xml version="1.0" encoding="utf-8"?>
<jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout
    android:id="@+id/refresh_sticky_expand_listview_layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/refresh_head"/>

    <jp.sinya.refreshlibrary.pullableview.PullableStickyExpandableListView
        android:id="@+id/refresh_sticky_expand_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@null"
        android:childDivider="#eee"
        android:childIndicatorLeft="0dp"
        android:divider="#BEBEBE"
        android:dividerHeight="1dp"
        android:groupIndicator="@null"
        android:listSelector="@android:color/transparent"
        android:scrollbarAlwaysDrawHorizontalTrack="false"/>

    <include layout="@layout/refresh_load_more"/>
</jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout>