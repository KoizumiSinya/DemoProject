<?xml version="1.0" encoding="utf-8"?>
<jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout
    android:id="@+id/refresh_sticky_swipe_expand_listview_layout"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <include layout="@layout/refresh_head"/>

    <jp.sinya.refreshlibrary.pullableview.PullableStickySwipeExpandableListView
        android:id="@+id/refresh_sticky_swipe_expand_listview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:cacheColorHint="@null"
        android:childDivider="#F0EFF5"
        android:childIndicatorLeft="0dp"
        android:divider="#F0EFF5"
        android:dividerHeight="1dp"
        android:groupIndicator="@null"
        android:listSelector="@android:color/transparent"
        android:scrollbarAlwaysDrawHorizontalTrack="false"/>

    <include layout="@layout/refresh_load_more"/>
</jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout>
