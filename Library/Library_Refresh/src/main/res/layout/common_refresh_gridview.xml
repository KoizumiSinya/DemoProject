<jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/refresh_gridview_layout"
    android:layout_width="match_parent"
    android:layout_height="match_parent" >

    <include layout="@layout/refresh_head" />
    <!-- 支持所有实现Pullable接口的View -->

    <jp.sinya.refreshlibrary.pullableview.PullableGridView
        android:id="@+id/refresh_gridview"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/white"
        android:columnWidth="90dp"
        android:gravity="center"
        android:horizontalSpacing="10dp"
        android:numColumns="auto_fit"
        android:stretchMode="columnWidth"
        android:verticalSpacing="15dp" />

    <include layout="@layout/refresh_load_more" />

</jp.sinya.refreshlibrary.pullableview.PullToRefreshLayout>